---
- name: Install apache
  hosts: all
  become: yes
  tasks:
    - name: Install apache
      ansible.builtin.apt:
        name: apache2
        update_cache: yes
        state: present
    - name: Copy file
      ansible.builtin.apt:
        name: index.html
        src: index.html
        dest: /var/www/html/
